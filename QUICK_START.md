# FormCritic - Quick Start Guide\n\n## 🚀 What You Have\n\n✅ Complete React Native/Expo app (iOS-first, Android ready)\n✅ AWS Lambda handler (Claude Vision integration)\n✅ Full TypeScript codebase\n✅ 8 documentation files\n✅ Ready for TestFlight deployment\n\n## ⚡ 5-Minute Setup\n\n### 1. Push to GitHub\n\n```bash\ncd form-critic-app\ngit remote add origin https://github.com/AresTriandos/form-critic-app.git\ngit push -u origin main\n```\n\n### 2. Deploy Lambda to AWS\n\n**Option A: Using CloudFormation (Recommended - One-click)**\n\n1. Go to AWS Console → CloudFormation → Create Stack\n2. Upload `cloudformation.yml`\n3. Fill in parameters:\n   - **AnthropicApiKey**: Get from https://console.anthropic.com (create if needed)\n   - **FunctionName**: `form-critic-analyzer`\n   - **Memory**: `1024` MB\n   - **Timeout**: `60` seconds\n4. Click Create Stack\n5. Wait ~2 minutes\n6. Copy the **FunctionUrl** from Outputs\n\n**Option B: Using CLI Script**\n\n```bash\nexport ANTHROPIC_API_KEY=sk-ant-...\nchmod +x DEPLOY_LAMBDA.sh\n./DEPLOY_LAMBDA.sh\n```\n\n### 3. Configure the App\n\nCreate `.env.local`:\n\n```bash\nEXPO_PUBLIC_LAMBDA_ENDPOINT=https://your-lambda-function-url/\n```\n\nReplace `your-lambda-function-url` with the actual URL from Lambda Function URL output.\n\n### 4. Test Locally\n\n```bash\nnpm install\nnpm start\n\n# On another terminal:\nnpm run ios      # macOS only\n# OR\nnpm run android  # Android\n```\n\nThe app will open in simulator/emulator.\n\n### 5. Deploy to TestFlight (iOS)\n\n```bash\neas login              # Use your Expo account\neas build --platform ios --profile preview\neas submit --platform ios --latest\n```\n\nWait for TestFlight review (~30 min - 2 hours).\n\n### 6. Deploy to Google Play (Android)\n\n```bash\neas build --platform android --profile production\neas submit --platform android --latest\n```\n\n## 📱 Testing the App\n\n1. **Record a workout**: Open app → Tap \"Record\" → Allow camera → Film yourself doing any exercise (5-15 sec)\n2. **Wait for analysis**: Lambda will process (~10-15 sec)\n3. **See results**: Form score (0-100), exercise type, feedback, improvement cues\n4. **Save**: Results stored on device (encrypted)\n\n## 🔗 API Flow\n\n```\nApp records video\n    ↓\nUploads as base64 to Lambda\n    ↓\nLambda extracts 6 frames\n    ↓\nSends to Claude Vision API\n    ↓\nClaude analyzes exercise + form\n    ↓\nReturns:\n  - Exercise name\n  - Form score (0-100)\n  - Detailed feedback\n  - 3-4 key improvement cues\n    ↓\nApp displays results\n```\n\n## 🔑 Environment Variables\n\n### App\n- `EXPO_PUBLIC_LAMBDA_ENDPOINT`: Your Lambda Function URL\n\n### Lambda (set in AWS)\n- `ANTHROPIC_API_KEY`: Your Claude API key\n\n## ❌ Troubleshooting\n\n### \"Cannot find Lambda endpoint\"\n- Check `.env.local` has `EXPO_PUBLIC_LAMBDA_ENDPOINT` set\n- Make sure it ends with `/`\n- Verify Lambda function is deployed and function URL is created\n\n### \"Claude API error\"\n- Check `ANTHROPIC_API_KEY` is set in Lambda environment\n- Verify key is valid and active\n- Check Lambda logs: `aws logs tail /aws/lambda/form-critic-analyzer --follow`\n\n### \"App crashes on camera access\"\n- Grant camera permissions in device settings\n- Check `app.json` has camera permissions (already configured)\n\n### \"Build fails\"\n- Delete node_modules: `rm -rf node_modules`\n- Clear cache: `npm cache clean --force`\n- Reinstall: `npm install`\n\n## 📊 Project Structure Quick Reference\n\n```\nform-critic-app/\n├── src/\n│   ├── app/(tabs)/\n│   │   ├── index.tsx         ← Home screen\n│   │   ├── history.tsx       ← View past workouts\n│   │   └── record/\n│   │       ├── camera.tsx    ← Record video\n│   │       ├── processing.tsx ← Loading state\n│   │       └── results.tsx   ← Show form score + feedback\n│   └── utils/aws.ts          ← Lambda client\n├── lambda/\n│   └── index.ts              ← Claude Vision handler\n├── DEPLOY_LAMBDA.sh          ← AWS deployment script\n├── cloudformation.yml        ← CloudFormation template\n└── .env.local               ← Your config (CREATE THIS)\n```\n\n## 🎯 What's Happening Under the Hood\n\n### App Flow\n1. User taps \"Record\"\n2. Opens camera\n3. Records 5-30 sec video\n4. Converts to base64\n5. POSTs to Lambda endpoint\n6. Waits for response (10-15 sec)\n7. Displays results\n8. Saves to device\n\n### Lambda Flow\n1. Receives base64 video\n2. Decodes to file\n3. Extracts 6 key frames (using FFmpeg)\n4. Sends frames to Claude 3.5 Sonnet Vision API\n5. Claude analyzes:\n   - What exercise is being performed?\n   - How is the form?\n   - What should improve?\n6. Returns structured JSON\n7. App displays results\n\n## 💾 Data Storage\n\n- **Videos**: Only uploaded temporarily, not stored\n- **Results**: Encrypted on device using Expo SecureStore\n- **History**: Grouped by date, sorted newest first\n\n## 🔒 Security\n\n- ✅ HTTPS only for Lambda communication\n- ✅ API keys never exposed in app\n- ✅ Local storage encrypted\n- ✅ No personal data logged\n\n## 📝 Important Notes\n\n1. **First build takes longer** (~5-10 min) due to native dependencies\n2. **TestFlight submission** needs Apple ID with developer account\n3. **Anthropic API key costs** ~$0.02 per analysis (Claude Vision API pricing)\n4. **Lambda free tier** covers first 1M requests/month\n\n## 🆘 Need Help?\n\n1. Check the comprehensive README.md\n2. Review DEVELOPMENT.md for detailed setup\n3. Check DEPLOYMENT_CHECKLIST.md for step-by-step AWS setup\n4. Read lambda/DEPLOYMENT.md for Lambda-specific details\n\n## Next Steps After Deployment\n\n- [ ] Push to GitHub\n- [ ] Deploy Lambda (CloudFormation or CLI)\n- [ ] Test locally on iOS simulator\n- [ ] Build for TestFlight\n- [ ] Submit to TestFlight\n- [ ] Iterate on form based on real usage\n\n---\n\n**Total Setup Time:** 15-20 minutes (first time)\n**Build + TestFlight Submission:** 10-15 minutes\n**TestFlight Review:** 30 min - 2 hours\n\nYou've got this! 🚀\n"
